# Official breaking changes we have actually hit

Empty at the first pin (`0.1.0-rc.6`). When contract CI fails on a new rc, record the delta here — do not invent a compatibility shim in Desktop IPC.

Template:

```
## 0.1.0-rc.N

- surface: cli | config-row | package | readiness
- what moved:
- community action: bump pin / edit dsh-bridge / update snapshot
```
