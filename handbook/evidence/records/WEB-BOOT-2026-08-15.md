# WEB-BOOT-2026-08-15

这是固定版本 Web 服务启动层的 F2 探针，不是端到端 Web UI Lab。

## 结果

复用已解析的 `@deepseek-ai/dsh@0.1.0-rc.6` 本地包，以只读模式启动 Web profile：

```text
dsh web: http://127.0.0.1:43367
```

服务在随机本机端口启动成功，随后由探针外层停止。随机端口只用于本次观察，不作为配置值保存。

这证明固定版本可以进入 Web profile 并完成启动公告；不证明浏览器渲染、Provider、模型、workspace、Trajectory 或任务执行正常。

对应 YAML 记录：[`WEB-BOOT-2026-08-15.yaml`](WEB-BOOT-2026-08-15.yaml)。
