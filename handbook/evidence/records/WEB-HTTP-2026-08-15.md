# WEB-HTTP-2026-08-15

这是固定版本 dsh Web profile 的本机 HTTP 层 F2 探针。

## 观察结果

服务在随机本机端口启动后，使用 curl 请求根路径：

```text
HTTP_STATUS=200
<!doctype html>
<title>DeepSeek Harness</title>
```

随后停止服务。该记录只证明 HTTP 根路径返回了 DeepSeek Harness HTML shell，不证明浏览器渲染、模型配置、workspace、session、Trajectory 或任务执行正常。

对应 YAML 记录：[`WEB-HTTP-2026-08-15.yaml`](WEB-HTTP-2026-08-15.yaml)。
